{% extends 'base.html.twig' %}

{% block title %}Confirmer la suppression du commentaire{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Êtes-vous sûr de vouloir supprimer ce commentaire ?</h1>

        <p><strong>Auteur :</strong> {{ commentaire.nomAuteur }}</p>
        <p><strong>Commentaire :</strong> {{ commentaire.texte }}</p>

        <div class="d-flex justify-content-between">
            <a href="{{ path('commentaires_etablissement', {'id': commentaire.etablissement.id}) }}" class="btn btn-secondary">Annuler</a>

            <form action="{{ path('commentaire_supprimer', {'id': commentaire.id}) }}" method="post">
                <button type="submit" class="btn btn-danger">Supprimer le commentaire</button>
            </form>
        </div>
    </div>
{% endblock %}
